/* ************************************************************************** *//*                                                                            *//*                                                        :::      ::::::::   *//*   main.c                                             :+:      :+:    :+:   *//*                                                    +:+ +:+         +:+     *//*   By: desausag <desausag@student.42.fr>          +#+  +:+       +#+        *//*                                                +#+#+#+#+#+   +#+           *//*   Created: 2021/03/20 18:02:25 by desausag          #+#    #+#             *//*   Updated: 2021/04/10 11:16:02 by desausag         ###   ########.fr       *//*                                                                            *//* ************************************************************************** */#include "./game/ft_game.h"unsigned int vis(t_all *all, t_int s_t, t_cord cross, t_cord ray){	int tmp1;	int wall;	if (s_t.i == 1)	{		if (ray.y > 0)			wall = 1;		else			wall = 0;		tmp1 = (int)((cross.x - floor(cross.x)) * all->txre_img[wall].w);	}	else if (s_t.i == 0)	{		if (ray.x > 0)			wall = 3;		else			wall = 2;		tmp1 = (int)((cross.y - floor(cross.y)) * all->txre_img[wall].w);	}	return (get_color(all->txre_img[wall], tmp1, s_t.j));}double	print_wall(t_cord cross, t_all *all, int x, t_cord ray, int side){	double d_to_wall;	t_cord y;	t_cord i_k;	double h;	t_int s_t;	i_k.x = -1;	s_t.i = side;	d_to_wall = len_ray(all->plr.pos, cross) * angle(all->plr.dir, ray);	h = all->textures.height/(d_to_wall);  // line height	y.x = all->textures.height/2 - h/2;	y.y = all->textures.height/2 + h/2;	i_k.y = all->txre_img[side].h / h;	while (++i_k.x <= all->textures.height)		if (i_k.x > y.x  && i_k.x < y.y)		{			s_t.j = (int) (i_k.y * (i_k.x - y.x));			my_mlx_pixel_put(all, x, i_k.x, vis(all, s_t, cross, ray));		}		else if (i_k.x <= y.x)			my_mlx_pixel_put(all, x, i_k.x, 0xAFEEEE);		else if (i_k.x >= y.y)			my_mlx_pixel_put(all, x, i_k.x, 0xCD853F);	return (y.x);}void	draw_screen(t_all *all){	t_cord b_x = {0, 1};//вектор по x	t_cord b_y = {1, 0};//вектор по y	t_cord cross;	t_cord cross_x;//вектор пересечения луча и х координаты	t_cord dot_b;//точка на векторе сетки	t_cord ray;	double 	zbuf[all->textures.width];	int i = 0;	char **map = all->map;	int side;	mlx_destroy_image(all->win.mlx, all->win.img);	init_img(all);	ray = rotateZ(all->plr.dir, -ANG/2, all->si_co);	all->ray0 = ray;	null_sprites(all);	while (i < all->textures.width)//привязать к ширине экрана	{		dot_b = net_point(ray, all->plr.pos);//точка пересечения сетки		cross = all->plr.pos;		while (!(is_wall_cord(map, cross, ray)))		{			cross_x = crc(ray, b_x, all->plr.pos, dot_b);//точка пересечения по х			cross = crc(ray, b_y, all->plr.pos, dot_b);//точка пересечения по у			if (len_ray(all->plr.pos, cross_x) < len_ray(all->plr.pos, cross))//если по х ближе чем по у			{				side = 0;				cross = cross_x;				dot_b = fwd_pnt(ray, dot_b, 1);			}			else //Если у ближе чем х			{				side = 1;                dot_b = fwd_pnt(ray, dot_b, -1);			}			t_cord cell;			if ((cell = is_sprite(map, cross, ray)).x != -1)				init_sprite(all, cell);		}		zbuf[i] = print_wall(cross, all, i, ray, side);        ray = rotateZ(ray, 0, all->si_co);		i++;	}	print_sprite(all, zbuf);	if (all->save == 1)		screen_image(all);	mlx_put_image_to_window(all->win.mlx, all->win.mlx_win, all->win.img, 0, 0);}int		main(int argc, char **argv) {    t_textures textures;    t_all all;    if (argc == 1 && check_ext(argv[1]))    	err("Error arguments\n");    if (!open_file(argv[1], &textures, &all))        err("Error file\n");    all.textures = textures;	all.plr = textures.plr;	all.map = textures.map;	all.save = 0;	if (argc == 3 && !(ft_strncmp(argv[2], "--save", 7)))			all.save = 1;	all.win.mlx = mlx_init();	check_display_resolution(&all);	all.win.mlx_win = mlx_new_window(all.win.mlx, textures.width, textures.height, "Cub3d!");	init_texture(&all);    init_img(&all);    all.si_co = v_set(cos(ANG*(M_PI/180)/all.textures.width), sin(ANG*(M_PI/180)/all.textures.width));	draw_screen(&all);    mlx_hook(all.win.mlx_win, 2, 1L<<0, key_hook, &all);	mlx_hook(all.win.mlx_win, X, 1L << 0, exit, 0);	mlx_loop_hook(all.win.mlx, draw_screen, &all);	mlx_loop(all.win.mlx);	return 0;}